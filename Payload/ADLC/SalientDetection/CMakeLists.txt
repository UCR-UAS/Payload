cmake_minimum_required(VERSION 2.8)
project( saliency_detecion  )
set(CUDA_USE_STATIC_CUDA_RUNTIME OFF)
find_package( OpenCV REQUIRED )
add_library(saliency_detection saliency_detection.cpp)
#add_executable( Test test.cpp )
target_link_libraries( saliency_detection ${OpenCV_LIBS})
